name: Deploy website to Cloudflare Workers

on:
  push:
    branches:
      - main
    paths:
      - apps/website/**
      - .github/workflows/deploy_website.yaml
  workflow_dispatch:

concurrency:
  group: deploy-website
  cancel-in-progress: false

permissions: {}

jobs:
  deploy:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write # Workload Identity Federation (OIDC) for Firestore auth.
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - id: flutter-version
        run: echo "version=$(jq -r .flutter .fvmrc)" >> "$GITHUB_OUTPUT"

      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: ${{ steps.flutter-version.outputs.version }}
          channel: stable
          cache: true

      - run: dart pub get

      # Authenticate to GCP via Workload Identity Federation (keyless). A
      # service-account access token bypasses App Check enforcement (App Check
      # only gates clients, not trusted backends), so the generator can read the
      # prod `sponsors` collection. Skipped until the WIF vars are configured —
      # then the generator falls back to the sample fixture (placeholders).
      - id: auth
        if: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER_PROD != '' }}
        uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3
        with:
          project_id: ${{ vars.PROD_FIREBASE_PROJECT_ID }}
          workload_identity_provider: ${{ vars.GCP_WORKLOAD_IDENTITY_PROVIDER_PROD }}
          service_account: ${{ vars.GCP_SERVICE_ACCOUNT_PROD }}
          token_format: access_token
          access_token_scopes: https://www.googleapis.com/auth/datastore

      # Generate the (git-ignored) sponsor data + images from the prod Firestore
      # project (packages/data `sponsors` collection). With no token (WIF not yet
      # configured) or if Firestore is unreachable, the generator falls back to
      # the sample fixture so the deploy still succeeds with placeholders.
      - name: Generate sponsor data
        run: dart run tool/generate_sponsors.dart
        env:
          FIREBASE_PROJECT_ID: ${{ vars.PROD_FIREBASE_PROJECT_ID }}
          FIRESTORE_HOST: firestore.googleapis.com
          FIRESTORE_ACCESS_TOKEN: ${{ steps.auth.outputs.access_token }}

      - working-directory: apps/website
        run: dart run jaspr_cli:jaspr build

      - uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: apps/website
          command: deploy
