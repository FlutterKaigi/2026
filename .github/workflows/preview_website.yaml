name: PR Preview Website

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - apps/website/**
      - .github/workflows/preview_website.yaml

concurrency:
  group: preview-website-${{ github.event.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  preview:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      pull-requests: write
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - id: flutter-version
        run: echo "version=$(jq -r .flutter .fvmrc)" >> "$GITHUB_OUTPUT"

      - uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2 # v2.23.0
        with:
          flutter-version: ${{ steps.flutter-version.outputs.version }}
          channel: stable
          cache: true

      - run: dart pub get

      # Generate the (git-ignored) sponsor data + images from the STG Firestore
      # project (packages/data `sponsors` collection, public read). If the vars
      # are unset or Firestore is unreachable, the generator falls back to the
      # sample fixture so the preview still builds with placeholders.
      - name: Generate sponsor data
        run: dart run tool/generate_sponsors.dart
        env:
          FIREBASE_PROJECT_ID: ${{ vars.STG_FIREBASE_PROJECT_ID }}
          FIRESTORE_HOST: firestore.googleapis.com
          FIRESTORE_API_KEY: ${{ secrets.STG_FIREBASE_API_KEY }}

      - working-directory: apps/website
        run: dart run jaspr_cli:jaspr build

      - id: deploy
        uses: cloudflare/wrangler-action@ebbaa1584979971c8614a24965b4405ff95890e0 # v4.0.0
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          workingDirectory: apps/website
          command: versions upload

      - uses: thollander/actions-comment-pull-request@24bffb9b452ba05a4f3f77933840a6a841d1b32b # v3.0.1
        with:
          message: |
            ## 🌍 Website Preview
            🔗 ${{ steps.deploy.outputs.deployment-url }}

            🔑 Commit: `${{ github.event.pull_request.head.sha }}`
          comment-tag: deploy-website
