name: captions_server
description: FlutterKaigi 2026 live captions server — WebSocket audio ingest, transcription, translation, and fan-out.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  dart_firebase_admin: ^0.4.1
  http: ^1.6.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^2.0.1
  web_socket_channel: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
