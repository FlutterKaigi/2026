/// Canonical production origin, used to build absolute URLs such as the OGP
/// `og:image` / `og:url`. Keep in sync with `web/CNAME` (the custom domain).
library;

const String siteOrigin = 'https://2026.flutterkaigi.jp';
