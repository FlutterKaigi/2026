name: dashboard
description: "FlutterKaigi 2026 管理ダッシュボード"
publish_to: "none"
resolution: workspace

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  # Shared data layer
  data:
    path: ../../packages/data

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
