import 'package:flutter/material.dart';

class StaffMemberListPage extends StatelessWidget {
  const StaffMemberListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('スタッフ（実装予定）'));
  }
}
