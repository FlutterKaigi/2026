import 'package:flutter/material.dart';

class TimelineEventListPage extends StatelessWidget {
  const TimelineEventListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('タイムライン（実装予定）'));
  }
}
