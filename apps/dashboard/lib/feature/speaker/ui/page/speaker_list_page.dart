import 'package:flutter/material.dart';

class SpeakerListPage extends StatelessWidget {
  const SpeakerListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('スピーカー（実装予定）'));
  }
}
