name: broadcaster
description: "FlutterKaigi 2026 audio broadcaster — captures line-in audio and streams PCM16 to the captions server."
publish_to: 'none'
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  record: ^6.2.1
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
