docs/superpowers/

# FVM Version Cache
.fvm/

# Dart / Flutter build output and caches
.dart_tool/
.packages
.flutter-plugins-dependencies
pubspec.lock
build/

# Firebase Emulator local state
.firebaserc
.firebase/
packages/data/firebase/emulator-data/
firebase-debug.log
firestore-debug.log
storage-debug.log
ui-debug.log
.env
.env.local
.env.*.local

# IDE / OS
.idea/
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
*.iml
.DS_Store

# Generated sponsor data + processed sponsor images.
# Real sponsor info is fetched at build time and must never be committed.
apps/website/lib/constants/generated_sponsors.dart
apps/website/web/images/logos/

# Generated staff data.
# Real staff info is fetched at build time and must never be committed.
apps/website/lib/constants/generated_staff.dart
# Generated session/timetable data.
# The programme is fetched at build time; it must not enter this public repo's
# git history before it is announced.
apps/website/lib/constants/generated_sessions.dart

# Generated Universal Links / App Links verification files
# (apple-app-site-association / assetlinks.json). Built from Repository
# Variables (APPLE_TEAM_ID / ANDROID_SHA256_FINGERPRINTS) at deploy time —
# see tool/generate_well_known.dart — and never committed.
apps/website/web/.well-known/
