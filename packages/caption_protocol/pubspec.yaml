name: caption_protocol
description: Versioned realtime caption protocol shared by FlutterKaigi venue surfaces.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
