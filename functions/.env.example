# Cloud Functions のデプロイ時パラメータ。
# このファイルを `.env` にコピーして値を設定する（`.env` は Git 管理外）。
#
#   cp functions/.env.example functions/.env
#
# 同期先（本番）の Firebase プロジェクト ID。
SYNC_TARGET_PROJECT_ID=

# Sign in with Apple のトークン失効用。プロジェクトごとに
# `.env.<project-id>` を作り、Firebase Console / Apple Developer の値を設定する。
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_SIGN_IN_CLIENT_ID=
