{
  "name": "flutterkaigi-2026-functions",
  "description": "FlutterKaigi 2026 Cloud Functions (STG → 本番 データ反映)",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean && tsc -p tsconfig.build.json",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "npm run clean && tsc && node --test lib/exchange/*.test.js"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
