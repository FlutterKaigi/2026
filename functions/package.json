{
  "name": "flutterkaigi-2026-functions",
  "description": "FlutterKaigi 2026 Cloud Functions (STG → 本番 データ反映)",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "tsc --noEmit",
    "test": "npm run build && node --test test/support_lt.test.cjs test/support_lt_auth.test.cjs",
    "test:coverage": "npm run build && node --test --experimental-test-coverage --test-coverage-include='lib/support_lt_*.js' --test-coverage-lines=80 --test-coverage-branches=80 --test-coverage-functions=80 test/support_lt.test.cjs test/support_lt_auth.test.cjs",
    "test:emulator": "node --test test/*.emulator.test.cjs"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
